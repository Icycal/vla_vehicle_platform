vla_policy_gateway:
  ros__parameters:
    transport: unix_socket
    socket_path: "/run/vla-policy/policy.sock"
    transport_timeout: 1.0
vla_debug_orchestrator:
  ros__parameters:
    transport: unix_socket
    socket_path: "/run/vla-policy/policy.sock"
    transport_timeout: 35.0
