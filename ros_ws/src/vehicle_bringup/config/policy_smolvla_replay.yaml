vla_policy_gateway:
  ros__parameters:
    transport: unix_socket
    socket_path: "/run/vla-policy/policy.sock"
    transport_timeout: 10.0
    prediction_frequency: 0.25
    observation_timeout: 5.0
    observation_topic: "/vla/replay/observation"
